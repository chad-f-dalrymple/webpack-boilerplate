require('./scss/app.scss');

// import the rest of the scss
// require.context('./scss/', true, /\.scss$/);

// do something
console.log('test');

// make sure jquery works
$('#page-title').text('jQuery is working');